import { HttpParamsType, HttpStatusCode, HttpMethod } from "../common";

export type UHttpStatusCode = `${HttpStatusCode}`;

export type UHttpParamsType = `${HttpParamsType}`;

export type UHttpMethod = `${HttpMethod}`;
